document.addEventListener("DOMContentLoaded",function(){const countdownDate=new Date("July 12, 2025 00:00:00").getTime();const daysElement=document.getElementById("days");const hoursElement=document.getElementById("hours");const minutesElement=document.getElementById("minutes");const secondsElement=document.getElementById("seconds");const countdownSection=document.querySelector(".countdown-section");const countdownStatus=document.getElementById("countdown-status");function updateCountdown(){const now=(new Date).getTime();const timeRemaining=countdownDate-now;const days=Math.floor(timeRemaining/(1e3*60*60*24));const hours=Math.floor(timeRemaining%(1e3*60*60*24)/(1e3*60*60));const minutes=Math.floor(timeRemaining%(1e3*60*60)/(1e3*60));const seconds=Math.floor(timeRemaining%(1e3*60)/1e3);daysElement.textContent=days;hoursElement.textContent=hours;minutesElement.textContent=minutes;secondsElement.textContent=seconds;if(countdownStatus){countdownStatus.textContent=`${days} days, ${hours} hours, ${minutes} minutes, and ${seconds} seconds until launch.`}if(timeRemaining<0){clearInterval(countdownTimer);daysElement.textContent="0";hoursElement.textContent="0";minutesElement.textContent="0";secondsElement.textContent="0";if(countdownStatus){countdownStatus.textContent="We have launched!"}if(countdownSection){const launchMessage=document.createElement("div");launchMessage.className="launch-message";launchMessage.textContent="We have launched!";launchMessage.setAttribute("role","status");launchMessage.setAttribute("aria-live","polite");countdownSection.appendChild(launchMessage)}}}updateCountdown();const countdownTimer=setInterval(updateCountdown,1e3);const logoElement=document.querySelector(".logo");if(logoElement){logoElement.addEventListener("click",function(){window.scrollTo({top:0,behavior:"smooth"})});logoElement.style.cursor="pointer";logoElement.setAttribute("role","button");logoElement.setAttribute("tabindex","0");logoElement.setAttribute("aria-label","Back to top");logoElement.addEventListener("keydown",function(event){if(event.key==="Enter"||event.key===" "){event.preventDefault();window.scrollTo({top:0,behavior:"smooth"})}})}});